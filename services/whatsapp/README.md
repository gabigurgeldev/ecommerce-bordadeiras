# WhatsApp service

Microserviço Baileys para envio de mensagens. Usa o mesmo banco via **Supabase** (`@supabase/supabase-js` + service role).

Env: `NEXT_PUBLIC_SUPABASE_URL` (ou `SUPABASE_URL`) e `SUPABASE_SERVICE_ROLE_KEY`.
