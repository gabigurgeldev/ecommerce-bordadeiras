-- Prisma PostgreSQL baseline (no DDL).
-- Schema is owned by supabase/migrations/*.sql (already applied on Supabase).
-- MySQL Prisma history: prisma/migrations/_archive_mysql/
--
-- On a DB where Supabase SQL is already applied, mark this migration without running DDL:
--   npx --yes prisma@6 migrate resolve --applied 20250603130000_postgresql_baseline

SELECT 1;
