# Database (MySQL)
DATABASE_URL="mysql://user:password@localhost:3306/bordadeiras"

# Auth.js
AUTH_SECRET="generate-with-openssl-rand-base64-32"
# Fallback admin access when user.role is not yet ADMIN in DB
ADMIN_EMAIL="admin@bordadeiras.com.br"

# Optional: object storage, redis, mercadopago (also configurable in /admin/configuracoes)
