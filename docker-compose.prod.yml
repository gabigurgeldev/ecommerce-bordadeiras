# Produção — Hostinger VPS / EasyPanel
# Uso: cp env.production.example .env && edite senhas → docker compose -f docker-compose.prod.yml up -d
#
# Hostnames na rede Docker (use no .env da app e do whatsapp-service):
#   redis              → porta 6379
#   minio              → API 9000, console 9001
#   whatsapp-service   → porta 4001
#   app                → porta 3000 (perfil "app"; omita se a loja rodar só no EasyPanel)
#
# Banco: Supabase self-hosted (fora do compose) — https://supabase.bordadeiras.cloud
# AVISO: Redis NÃO publica portas no host. Não mapeie 6379 para 0.0.0.0 em produção.

name: bordadeiras-prod

services:
  redis:
    image: redis:7-alpine
    container_name: bordadeiras-redis
    command: redis-server --appendonly yes
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    read_only: true
    tmpfs:
      - /tmp:size=32m,mode=1777
    mem_limit: 256m
    cpus: "0.50"
    pids_limit: 100
    volumes:
      - redis_data:/data
    networks:
      - bordadeiras
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 3s
      retries: 5
    restart: unless-stopped
    # Sem ports: — acesso apenas na rede Docker

  minio:
    image: minio/minio:latest
    container_name: bordadeiras-minio
    command: server /data --console-address ":9001"
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    tmpfs:
      - /tmp:size=128m,mode=1777
    mem_limit: 768m
    cpus: "1.00"
    pids_limit: 200
    environment:
      MINIO_ROOT_USER: ${S3_ACCESS_KEY:?Defina S3_ACCESS_KEY no .env de producao}
      MINIO_ROOT_PASSWORD: ${S3_SECRET_KEY:?Defina S3_SECRET_KEY no .env de producao}
    volumes:
      - minio_data:/data
    networks:
      - bordadeiras
    expose:
      - "9000"
      - "9001"
    restart: unless-stopped
    # Publique 9000/9001 via reverse proxy (EasyPanel) em storage.seudominio.com — não use 0.0.0.0:9000 em VPS público

  whatsapp-service:
    build:
      context: .
      dockerfile: services/whatsapp/Dockerfile
    container_name: bordadeiras-whatsapp
    init: true
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    read_only: true
    tmpfs:
      - /tmp:size=128m,mode=1777
    mem_limit: 512m
    cpus: "0.75"
    pids_limit: 200
    env_file:
      - .env
    environment:
      PORT: "4001"
      NODE_ENV: production
      NEXT_PUBLIC_SUPABASE_URL: ${NEXT_PUBLIC_SUPABASE_URL:?Defina NEXT_PUBLIC_SUPABASE_URL no .env}
      SUPABASE_URL: ${NEXT_PUBLIC_SUPABASE_URL:?Defina NEXT_PUBLIC_SUPABASE_URL no .env}
      SUPABASE_SERVICE_ROLE_KEY: ${SUPABASE_SERVICE_ROLE_KEY:?Defina SUPABASE_SERVICE_ROLE_KEY no .env}
      WHATSAPP_SERVICE_SECRET: ${WHATSAPP_SERVICE_SECRET:?Defina WHATSAPP_SERVICE_SECRET forte no .env}
    volumes:
      - whatsapp_auth:/app/data/auth
    networks:
      - bordadeiras
    expose:
      - "4001"
    healthcheck:
      test:
        [
          "CMD-SHELL",
          'node -e "fetch(''http://127.0.0.1:4001/health'').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))"',
        ]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 40s
    restart: unless-stopped

  app:
    profiles: ["app"]
    build:
      context: .
      dockerfile: Dockerfile
    container_name: bordadeiras-app
    init: true
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    read_only: true
    tmpfs:
      - /tmp:size=128m,mode=1777
      - /app/.next/cache:size=256m,uid=1001,gid=1001,mode=0770
    mem_limit: 1g
    cpus: "1.00"
    pids_limit: 300
    env_file:
      - .env
    environment:
      NODE_ENV: production
    networks:
      - bordadeiras
    depends_on:
      redis:
        condition: service_healthy
      minio:
        condition: service_started
      whatsapp-service:
        condition: service_started
    expose:
      - "3000"
    healthcheck:
      test:
        [
          "CMD-SHELL",
          'node -e "fetch(''http://127.0.0.1:3000'').then(r=>process.exit(r.status<500?0:1)).catch(()=>process.exit(1))"',
        ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    restart: unless-stopped
    # No EasyPanel, prefira build/deploy da app pelo painel e domínio loja.seudominio.com

  # Postal SMTP — descomente se for self-hosted na mesma VPS
  # postal:
  #   image: postalserver/postal:latest
  #   profiles: ["postal"]
  #   networks: [bordadeiras]
  #   volumes: [postal_data:/opt/postal]
  #   restart: unless-stopped

networks:
  bordadeiras:
    name: bordadeiras_internal
    driver: bridge

volumes:
  redis_data:
  minio_data:
  whatsapp_auth:
  # postal_data:
